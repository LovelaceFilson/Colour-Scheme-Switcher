function defaultOne(color) {
    document.body.style.background = color;  
  }
  
  function defaultTwo(color) {
    document.body.style.background = color;  
  }
  
  function defaultThree(color) {
    document.body.style.background = color;  
  }
  
  function defaultFour(color) {
    document.body.style.background = color;  
  }
  